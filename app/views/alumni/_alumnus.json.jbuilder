json.extract! alumnus, :id, :uin, :cohort_year, :team_affiliation, :profession_title, :availability, :email, :phone_number, :biography, :created_at, :updated_at
json.url alumnus_url(alumnus, format: :json)
